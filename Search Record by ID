#include <stdio.h>

struct Item {
    int id;
    char name[20];
};

int main() {
    int n, i, sid;
    struct Item it[10];

    scanf("%d", &n);

    for(i = 0; i < n; i++)
        scanf("%d %s", &it[i].id, it[i].name);

    scanf("%d", &sid);

    for(i = 0; i < n; i++) {
        if(it[i].id == sid) {
            printf("Name: %s", it[i].name);
            return 0;
        }
    }
    printf("Not Found");
    return 0;
}
