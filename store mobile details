#include <stdio.h>

struct mobile {
    char brand[20];
    int price;
};

int main() {
    struct mobile m;
    printf("Enter brand and price: ");
    scanf("%s %d", m.brand, &m.price);

    printf("Brand: %s Price: %d\n", m.brand, m.price);
    return 0;
}
