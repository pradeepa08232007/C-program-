#include <stdio.h>

struct student {
    int id;
    char name[20];
};

int main() {
    struct student s;
    printf("Enter ID and Name: ");
    scanf("%d %s", &s.id, s.name);

    printf("ID: %d Name: %s\n", s.id, s.name);
    return 0;
}
