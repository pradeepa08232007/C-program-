#include <stdio.h>

struct Student {
    int roll;
    char name[20];
};

int main() {
    int n;
    printf("Enter number of records: ");
    scanf("%d", &n);

    printf("Total records = %d", n);
    return 0;
}
