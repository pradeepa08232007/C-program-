#include <stdio.h>

struct bank {
    int accNo;
    float balance;
};

int main() {
    struct bank b;
    printf("Enter account number and balance: ");
    scanf("%d %f", &b.accNo, &b.balance);

    printf("Account No: %d Balance: %.2f\n", b.accNo, b.balance);
    return 0;
}
