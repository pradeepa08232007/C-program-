#include <stdio.h>
int main() {
    float w, h, bmi;
    printf("Enter weight (kg) and height (m): ");
    scanf("%f %f", &w, &h);
    bmi = w / (h * h);
    printf("BMI = %.2f", bmi);
    return 0;
}
