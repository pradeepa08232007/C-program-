#include <stdio.h>

struct Customer {
    int id;
    char name[30];
    char mobile[15];
};

int main() {
    int n, i, searchId;
    struct Customer c[10];

    printf("Enter number of customers: ");
    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d %s %s", &c[i].id, c[i].name, c[i].mobile);
    }

    printf("Enter ID to search: ");
    scanf("%d", &searchId);

    for(i = 0; i < n; i++) {
        if(c[i].id == searchId) {
            printf("Name: %s\nMobile: %s", c[i].name, c[i].mobile);
            return 0;
        }
    }
    printf("Customer not found");
    return 0;
}
