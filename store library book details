#include <stdio.h>

struct book {
    int id;
    char title[20];
};

int main() {
    struct book b;
    printf("Enter book id and title: ");
    scanf("%d %s", &b.id, b.title);

    printf("Book ID: %d Title: %s\n", b.id, b.title);
    return 0;
}
